<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Genres</title>
  </head>
  <body>
    <%- include('layout/header') %>
    <h1>Genres Page</h1>
    <% if (genres) { %> <%genres.forEach(genres => {%>
    <h2><%= genres.name %></h2>
    <%})%> <%}%> <%- include('layout/footer') %>
  </body>
</html>